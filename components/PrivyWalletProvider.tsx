// Unused — kept as placeholder. App uses WalletProvider.tsx (Solana wallet adapter) instead.
export default function PrivyWalletProvider({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
